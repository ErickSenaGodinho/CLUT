name: C/C++ CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc

    - name: Compile runner generator
      run: gcc -o runner_generator tools/runner_generator.c

    - name: Generate test core runner
      run: ./runner_generator -o tests/core/runner_test_core.c tests/core/test_basic.h tests/core/test_numeric.h tests/core/test_string.h tests/core/test_arrays.h

    - name: Compile test core runner
      run: gcc -o tests/core/runner_test_core tests/core/runner_test_core.c -DCLUT_OUTPUT_GITHUB

    - name: Run core tests
      run: ./tests/core/runner_test_core
